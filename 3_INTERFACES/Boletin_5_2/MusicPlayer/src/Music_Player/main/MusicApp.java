package Music_Player.main;

public class MusicApp {

    /*
    * public class MusicApp {

    public void startMusic(MusicPlayer[] players) {
        for (MusicPlayer player : players) {
            if (player instanceof OnlineMusicPlayer) {
                OnlineMusicPlayer onlinePlayer = (OnlineMusicPlayer) player;
                onlinePlayer.stream();
            } else if (player instanceof OfflineMusicPlayer) {
                OfflineMusicPlayer offlinePlayer = (OfflineMusicPlayer) player;
                offlinePlayer.load();
            }
            player.play();
            player.stop();
        }
    }
}
*/

}
